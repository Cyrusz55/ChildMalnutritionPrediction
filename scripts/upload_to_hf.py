"""
This script was intentionally removed: use the README at the repository root to upload models
to the Hugging Face Hub using the terminal (recommended).

Reason: uploading via the terminal with `huggingface-cli` + `git` + `git lfs` is simpler
and does not require storing a token in code. See ../README.md for step-by-step instructions.
"""

if __name__ == '__main__':
    print("upload_to_hf.py has been removed. See README.md for terminal instructions to upload models to Hugging Face.")


